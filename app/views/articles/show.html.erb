<h1><%= @article.title %></h1>

<p><%= @article.text %></p>

<%= form_for([@article, @article.comments.build]) do |f| %>
 
 
    <%= f.label :author %><br>
    <%= f.text_field :author %>
 
    <%= f.label :body %><br>
    <%= f.text_area :body %>

    <%= f.submit %>

  <% end %>